#ifndef COMPUTE_H
#define COMPUTE_H

#include <math.h>
#include <stdlib.h>

#include "queue.h"
#include "stack.h"

double result(const queue *head, double value);  // подсчёт выражения (в польской нотации) из очереди

#endif